[pytest]
# Run from the project root: pytest
testpaths = tests
addopts =
    -v
    --tb=short
    -m "not realdb"

markers =
    realdb: Tests that hit the live PostgreSQL database (require DATABASE_URL=postgresql://...)