version: '3.8'

services:
  fastapi:
    build: .
    container_name: sa-backend
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - API_KEYS=${API_KEYS}
      - PORT=8000
    restart: unless-stopped